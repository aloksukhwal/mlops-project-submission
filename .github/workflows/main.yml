name: CI/CD Pipeline - Self-Hosted try

on:
  push:
    branches:
     - master

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v2
